const game = (function () {
  const gameBoard = {
    board: ["", "", "", "", "", "", "", "", ""],
  };

  const Players = function (name, marker, winState = false) {
    return { name, marker, winState };
  };

  

})();

game.render();
